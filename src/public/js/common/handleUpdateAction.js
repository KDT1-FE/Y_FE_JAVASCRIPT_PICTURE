export function handleUpdateAction(updateEl) {
  const memberForUpdate = updateEl.closest('.members__row')
  const memberId = memberForUpdate.getAttribute('data-id')
  memberId = id
  return memberId
}
